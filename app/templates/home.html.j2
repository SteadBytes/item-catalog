{% extends "base.html.j2" %}
{% block content %}
  <!-- {% with messages = get_flashed_messages() %}
    {% if messages %}
      <ul class=flashes>
      {% for message in messages %}
        <li>{{ message }}</li>
      {% endfor %}
      </ul>
    {% endif %}
  {% endwith %} -->
  <div class="row">
    <div class="small-2 medium-4 columns category-list">
      {%include 'category_list.html.j2'%}
    </div>

    <div class="small-8 medium-8 columns">
      <div class="latest-items">
        <h1>Latest Items</h1>
        <ul class="no-bullet ">
          {% for item in top_items %}
            <li>
              <a href="{{url_for('catalog.get_item', category_name=item.category.name, item_title=item.title)}}">
                {{item.title}}
              </a>
              <a class="subheader" href="{{url_for('catalog.items_by_category',category_name=item.category.name)}}">
              ({{item.category.name}})
              </a>
            </li>
          {% endfor %}
        </ul>
      </div>
    </div>
  </div>

{% endblock %}
